# Task 1 — Symbolic (JSB Chorales)
numpy>=1.24
torch>=2.0
matplotlib>=3.7
music21>=9.1
pretty_midi>=0.2.10
scipy>=1.10

# Task 4 — MusicGen evaluation (local re-run needs FMA training WAVs)
soundfile>=0.12
librosa>=0.10
scikit-learn>=1.3

# Optional: full AudioCraft fine-tuning (alternative to HuggingFace path)
# audiocraft  # pip install audiocraft (requires GPU, ~4GB)
